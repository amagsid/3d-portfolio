'use client';
import { Canvas } from '@react-three/fiber';
import Model from './Model';

const FloatingShape = () => {
    return (
        // <div>floating</div>
        // Models
        // ENvironment

        <Canvas>
            <Model />
        </Canvas>
    );
};

export default FloatingShape;
